// ============================================
// Query Keys — untuk future React Query / TanStack Query
// ============================================

export const queryKeys = {
  questions: ['questions'],
  assessment: (id) => ['assessment', id],
}
